# lesson_podcasts/ — INDEX

34 podcast **scripts** (`{lesson_id}_podcast.md`, Area I only). Scripts are text and live in git;
the produced **audio lives in Google Drive**, not this repo.

⚠️ Currently **orphaned**: no ingest script exists for podcasts (open task — see
`_my_resources/open_tasks/todo_list.md`). 14 lessons have no podcast (all non-Area-I +
`PPL_PA_I_H_04`).
