# quiz_banks_legacy_2026-07-23/ — INDEX

Frozen 2026-07-23 pre-rebalance snapshot of all 48 `{lesson_id}_quiz.json` banks (verified
byte-identical at copy time). Purpose + delete condition: `README.md` here. Nothing reads this
folder — `config.QUIZ_BANKS_DIR` still points at `../quiz_banks/`. Temporary: removed at story
6-3 phase R7 on Daniel's explicit word.
