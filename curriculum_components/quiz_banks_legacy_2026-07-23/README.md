# quiz_banks_legacy_2026-07-23/ — frozen pre-rebalance snapshot

Byte-identical copy of `../quiz_banks/*.json` taken 2026-07-23 **before** the story 6-3 answer-key
re-balance (see `_artifacts/2026-07-23_quiz-rebalance-firestore-truth/`). Read-only safety net —
no tool globs this folder. Delete after R6 verification, only on Daniel's explicit word (R7).
