# docs/project_context_prps/ — INDEX

Broader product / architecture context: **what the downstream app is** and why the curriculum is
shaped this way. Background reading — the app repo (`../AGY_AVIATIONCHAT/`) is the live source of
truth for anything app-side.

For how the two repos work together day to day, read `docs/SOP_curriculum_operations.md` instead.
