# specialist_curriculum/ — INDEX  ⚠️ LEGACY / DEPRECATED

The **old** home of the curriculum masters. The canonical locations are now
`curriculum_components/curriculum_modules/` (masters) and `pipeline/curriculum/` (DB1 source store);
the `.gitignore` still carries the note "the dir moved from `specialist_curriculum/`".

What remains here is a single leftover: `1 ACS Curriculum Key.json`, **byte-identical** (md5
`8ab095f7…`) to the live copy at `pipeline/curriculum/1 ACS Curriculum Key.json`. Nothing reads this
tree.

**Recommended: delete this folder** — flagged for Daniel's go-ahead (deleting files is a gated
action). Until then, treat it as read-only history and never author here.
