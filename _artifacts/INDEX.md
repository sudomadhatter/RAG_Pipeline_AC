# _artifacts/ INDEX — session ledger (RAG_Pipeline_AC)

One row per session, newest at the bottom.

| Date | Slug | Summary | Status |
|---|---|---|---|
| 2026-06-16 → 06-19 | (11 migrated folders) | Pre-conversion history moved from `_claude_artifacts/` on 2026-07-22 (`git mv`, history intact): artifact rules · quiz + bridge-key pipeline fixes · bridge ground-truth fix · artifact path scheme · overview + stranded lesson · curriculum cleanup · repo structure cleanup · state-map wiring. | migrated |
| 2026-07-22 | house-standard-conversion | Converted to the house standard from the command center: pointer front doors (`CLAUDE.md`/`GEMINI.md`), Layer-2 `AGENTS.md`, vendored `.agents/` toolkit (19 master rules incl. `operator-profile`), artifacts consolidated here, GitNexus governance dropped (de-grouped from ac-stack, local index deleted), md-feedback MCP wired, project rules added (`constitution.project.md`, `credential-resolution.md`). Single `main` branch **by design**. Full session record: home base `_artifacts/_main/2026-07-22_pipeline-conversion-and-sop/`. | done |
| 2026-07-23 | quiz-rebalance-firestore-truth | Story 6-3 plan: fix the answer-key skew (live audit confirmed B 67% in Firestore, zero content drift repo↔live, 206 empty `sjt_rationale` fossils, manifests clean). R1 legacy snapshot → R2 deterministic re-letter (2 per letter per bank) → R3 letter-free prose re-anchor → R4 permanent distribution test → R5 fossil deletion → R6 ingest+prove → R7 delete legacy. **Approved + CODE-COMPLETE 2026-07-23:** corpus 96×4 (`r2_letter_map.md` here) · 345 feedback fields letter-free across all 48 banks · suite 126 passed · 0 fabricated facts vs legacy. R3 finished inline by surgical positional replacement after the multi-agent fleet hit the session limit 3× (~5.6M tokens, 19/48 banks). **SHIPPED:** approved + spot-checked, `--execute` run — 384 questions / 48 lessons ingested, 292 fossil DELETE_FIELDs sent. Live proof: **96×4 (25% each)**, drift **identical=48 / differs=0** (fossils gone), manifests 48, `seen_by` untouched, state map clean, bridge **48/48**. Safety now D:24 (was zero). **Only R7 left: delete the legacy snapshot on Daniel's word.** | shipped — R7 pending |
