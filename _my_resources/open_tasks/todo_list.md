# Open Tasks — RAG_Pipeline_AC (Daniel's list)

> Daniel's notes. Agents: READ-ONLY — never edit; cross-check against live project files.
> (Seeded 2026-07-22 from the conversion audit's debt register — edit/reorder freely.)

- [ ] Author the remaining ACS Areas: II, IV, V, VIII, XII (Area X = N/A for single-engine PPL)
- [ ] Podcast ingestion: 34 scripts exist (Area I only) with NO ingest script — decide the target
      store, build the gated tool, backfill the 14 missing lessons
- [ ] 13 reference-only lessons cite sources not in DB2 (AME Guide, FCC forms, legal
      interpretations, FAA Orders) — add those sources to DB2 or accept semantic fallback
- [ ] Phase-2 folder merge: unify `pipeline/` + `curriculum_components/` under one `data/` root
      (PRD/README note) — big rename, needs its own plan
