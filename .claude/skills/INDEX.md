# Skills INDEX — RAG_Pipeline_AC (deliberately LEAN)

This repo carries a **lean skill set by decision** (Daniel, 2026-07-22): curriculum authoring +
generic hygiene only — no sudo dev-flow, no autopilots, no app-specific skills, no BMAD testing
family. The full house library lives at the lobby master (`c:\Sudo_Hatter_Command\.agents\skills\`);
hand-copy a specific skill in if a task truly needs it. **Do NOT blanket re-sync:** a full
`/sync-agents -Target` here re-imports the whole kit — re-prune to this keep-list afterwards.

| Skill | Use it for |
|---|---|
| `rkp-manifest-creation` | Author an RKP manifest from a master module (invokes the grounding gate) |
| `quiz-bank-generation` | Author a quiz bank from a lesson's RKP manifest (invokes the grounding gate) |
| `bridge-key-verification` | Verify bridge keys vs the DB2 vocabulary + live DB1→DB2 round-trip |
| `faa-grounding-gate` | Every factual claim traces to ACS/FAA sources ONLY — never model memory |
| `workspace-structure` | House files-and-folders standard when creating/moving/reorganizing |
| `systematic-debugging` | Debugging discipline for pipeline script issues |
| `update-maps-indexes` | Repo-map + INDEX reconciliation for this workspace |
| `python_inter_venv_fix` | VS Code interpreter/venv repair after moves/re-clones |

Mirror: `.claude/skills/` carries the same set (Claude resolves skills there). Keep the two in step.
