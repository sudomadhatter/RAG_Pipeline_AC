---
title: "Bridge Key Guide — MOVED (this copy was outdated)"
type: redirect
date: 2026-06-14
---

# ⚠️ This copy is outdated — see the canonical guide

This file previously contained a **v1 draft that was wrong** — it blamed the empty Area IX bridge keys
on a markdown/regex format mismatch. The real cause is an **LLM extractor + a schema that allows empty
keys silently** (not formatting).

**Read the corrected, canonical version here:**

➡️ **`_01_My/bridge_key_guide.md`** (same repo, one level up)

It contains the standing contract (what the app team needs on every batch), the verified root cause,
the document-level keying rules, the verification probe, and the Area IX fix procedure.
